# Dependencies & build output
node_modules
.next
out

# Local env
.env
.env.local

# Runtime, per-user state (private chat histories churn a lot)
workspace/users/

# Regenerable vector index (rebuild with `npm run index`)
workspace/index/

# Derived per-project kickoff-brief cache (regenerates on demand from memory)
workspace/projects/*/kickoff.json

# OS
.DS_Store

# TypeScript build cache
*.tsbuildinfo
