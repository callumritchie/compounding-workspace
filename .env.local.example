# Copy this file to ".env.local" and paste in your own Anthropic API key.
# Get a key at https://console.anthropic.com  → Settings → API Keys.
#
# .env.local is git-ignored, so your key stays private and never gets committed.

ANTHROPIC_API_KEY=sk-ant-your-key-here
